LOG "Initialization of variables"

IF count_times == None THEN
	LOG "state_1 is null"
	SET count_times = 1 
	LOG f"now it is: {count_times}"
ELSE 
	LOG f"count_times is not null {count_times}" 
ENDIF

SET count_times = count_times + 1

LOG "Initialization of variables"